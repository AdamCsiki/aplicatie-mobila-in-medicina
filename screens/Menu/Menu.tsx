import style from "./Menu.style";
import { Text, View } from "react-native";

function Menu({ shown }: { shown: boolean }) {
	return (
		<View>
			<Text>Menu</Text>
		</View>
	);
}

export default Menu;

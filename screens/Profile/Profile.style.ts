import { StyleSheet } from "react-native";

export default StyleSheet.create({
	Profile: {
		borderColor: "red",
		borderWidth: 1,

		width: "100%",

		display: "flex",
		flexDirection: "column",
		alignItems: "center",
	},
});

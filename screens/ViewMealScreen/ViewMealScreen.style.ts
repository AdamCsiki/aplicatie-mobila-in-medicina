import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    ViewMealScreen: {
        display: 'flex',

        padding: 16,

        flexGrow: 1,
    },
    FoodContainer: {
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
    },
})

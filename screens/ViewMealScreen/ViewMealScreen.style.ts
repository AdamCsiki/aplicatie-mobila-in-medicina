import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    ViewMealScreen: {},
    FoodContainer: {
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
    },
})

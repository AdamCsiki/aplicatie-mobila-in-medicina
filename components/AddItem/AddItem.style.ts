import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    AddItem: {
        borderRadius: 16,

        height: 75,
        width: '100%',

        padding: 0,
        margin: 0,

        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
    },
    Plus: {
        borderRadius: 16,

        height: 50,
        width: 50,

        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',

        overflow: 'hidden',
    },
})

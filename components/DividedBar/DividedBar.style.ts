import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    DividedBar: {
        maxHeight: 4,

        flexGrow: 1,

        borderRadius: 16,

        display: 'flex',
        flexDirection: 'row',
        justifyContent: 'flex-start',
        alignItems: 'center',

        overflow: 'hidden',
    },
})

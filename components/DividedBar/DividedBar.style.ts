import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    DividedBar: {
        height: 8,
        width: '100%',

        borderRadius: 16,

        display: 'flex',
        flexDirection: 'row',
        justifyContent: 'flex-start',
        alignItems: 'center',

        overflow: 'hidden',
    },
})

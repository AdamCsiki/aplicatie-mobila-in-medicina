import { StyleSheet } from "react-native";

export default StyleSheet.create({
	LogoContainer: {
		display: "flex",
		flexDirection: "row",
		justifyContent: "center",
		alignItems: "center",
	},
	Logo: {
		maxHeight: 40,
		maxWidth: 40,
	},
});

import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    ContentContainer: {
        borderRadius: 4,
        padding: 8,

        overflow: 'hidden',
    },
    List: {
        width: '100%',

        overflow: 'hidden',

        borderRadius: 4,
    },
})
